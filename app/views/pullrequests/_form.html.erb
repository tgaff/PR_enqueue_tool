<%= form_for(@pullrequest) do |f| %>
  <% if @pullrequest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pullrequest.errors.count, "error") %> prohibited this pullrequest from being saved:</h2>

      <ul>
      <% @pullrequest.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
